schema {
  query: Query
  mutation: Mutation
}

scalar DateTime

interface Node {
  id: ID
}

interface Timestamp {
  Created_At: DateTime
  Updated_At: DateTime
}

enum SortDirection {
  ASC
  DESC
}
